#!/usr/bin/node

/**
 * class Rectangle that defines a rectangle
 * Constructor takes 2 arguments, w, h
 * Instance attribute width = w, height = h
*/

module.exports = class Rectangle {
  constructor (w, h) {
    this.width = w;
    this.height = h;
  }
};
